function splitify(str) {
  // Only change code below this line
  const re = /[ ,-.]/
const newStr = str.split(re)

return newStr

  // Only change code above this line
}
console.log(splitify("Hello World,I-am code"))
;