const firstName = "Refki"
const lastName = "Santriono"
const template = `Name: ${firstName} ${lastName}`

console.info(template)

const name = "Refki Santriono"
const nilai = 80
const hasil = `Name: ${name} Lulus : ${nilai > 90}`


const hasilMultiline = `Name: ${name} 
Lulus : ${nilai > 90}`
console.info(hasilMultiline);

